---
/*

Arreglar el llevar los tags como props

interface Props {
  image: string;
  title: string;
  description:  string;
  date: string;
  tags:any;
}


const { image, title, description, date, tags: Tags} = Astro.props;

---
<div class="lg:w-1/3 sm:w-1/2 p-4">
    <div class="flex relative size-72">
      <img alt="gallery" class="absolute inset-0 w-full h-full object-cover object-center" src={image}>
      <div class="px-8 py-10 relative z-10 w-full border-0  bg-white opacity-0 hover:opacity-100" style="background-image: linear-gradient(45deg, #706269, #988e8c, #c2bcb1, #ededd8)">
        <h2 class="tracking-widest text-2xl title-font font-subtitles text-background mb-1">{title}</h2>
        <h1 class="title-font text-lg font-medium text-gray-900 mb-3">{date}</h1>
        <p class="leading-relaxed">{description}</p>
      </div>
    </div>
    <div class="flex flex-wrap mt-4 gap-3">
        {Tags}
    </div>
</div>
*/